import { expressUsersSellers } from './express-users-sellers';

describe('expressUsersSellers', () => {
  it('should work', () => {
    expect(expressUsersSellers()).toEqual('express-users-sellers');
  });
});
