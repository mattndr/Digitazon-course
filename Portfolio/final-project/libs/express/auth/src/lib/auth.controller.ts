export const login = async (req, res) => {
  const courses = await res.send('all courses');
};

export const signup = async (req, res) => {
  const courses = await res.send('all courses');
};

export const signout = async (req, res) => {
  const courses = await res.send('all courses');
};
